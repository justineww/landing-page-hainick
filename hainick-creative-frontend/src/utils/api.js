// Ganti value ini aja kalau pindah dari local ke server online (misal ke domain Hostinger)
export const BASE_URL = "http://localhost:8000";

// Semua route di Laravel dibungkus prefix /api/hainickkreatif, jadi digabung sekalian di sini
export const API_URL = `${BASE_URL}/api/hainickkreatif`;
